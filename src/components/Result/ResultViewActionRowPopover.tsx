import {Popover} from "antd";


function ResultViewActionRowPopover() {
  return (
    <Popover
      // content={<a onClick={true}>Close</a>}
      title="Title"
      trigger="click"
      // open={open}
      // onOpenChange={handleOpenChange}
    >
    </Popover>
  );
}

export default ResultViewActionRowPopover;